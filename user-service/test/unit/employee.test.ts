import { prismaMock } from "../../src/singleton";
import { v4 as uuid } from "uuid";

test("Should create new employee", () => {});
test("Should fail if id is not uuid on create", () => {});
test("Throw if employee.name is empty on create", () => {});
test("Throw if employee.role is empty on create", () => {});
test("Throw if employee.kontoNr is empty on create", () => {});
test("Throw if employee.regNr is empty on create", () => {});
test("Should update employee", () => {});
test("Should fail if id is not uuid on update", () => {});
test("Throw if employee.name is empty on update", () => {});
test("Throw if employee.role is empty on update", () => {});
test("Throw if employee.kontoNr is empty on update", () => {});
test("Throw if employee.regNr is empty on update", () => {});
test("Should get employee by id", () => {});
test("Should fail when getting employee with an invalid id", () => {});
test("should delete employee by id", () => {});
test("should fail when deleting a employee with an invalid id", () => {});
